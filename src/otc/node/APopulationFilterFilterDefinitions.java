/* This file was generated by SableCC (http://www.sablecc.org/). */

package otc.node;

import java.util.*;
import otc.analysis.*;

@SuppressWarnings("nls")
public final class APopulationFilterFilterDefinitions extends PFilterDefinitions
{
    private final LinkedList<PFilterList> _filterList_ = new LinkedList<PFilterList>();

    public APopulationFilterFilterDefinitions()
    {
        // Constructor
    }

    public APopulationFilterFilterDefinitions(
        @SuppressWarnings("hiding") List<PFilterList> _filterList_)
    {
        // Constructor
        setFilterList(_filterList_);

    }

    @Override
    public Object clone()
    {
        return new APopulationFilterFilterDefinitions(
            cloneList(this._filterList_));
    }

    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAPopulationFilterFilterDefinitions(this);
    }

    public LinkedList<PFilterList> getFilterList()
    {
        return this._filterList_;
    }

    public void setFilterList(List<PFilterList> list)
    {
        this._filterList_.clear();
        this._filterList_.addAll(list);
        for(PFilterList e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._filterList_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._filterList_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PFilterList> i = this._filterList_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PFilterList) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
